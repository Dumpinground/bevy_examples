[package]
name = "spaceship_game"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dev-dependencies]
bevy = { version = "0.13", features = ["dynamic_linking"]}

[dependencies]
bevy = "0.13"
